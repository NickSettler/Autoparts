<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.chenk.autoparts">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminEditPartsActivity" android:label="Редактирование запчастей"></activity>
        <activity android:name=".UserOrdersActivity" />
        <activity android:name=".UserEditActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".AdminCreatePartActivity" />
        <activity
            android:name=".OrderActivity"
            android:label="Оформление заказа" />
        <activity
            android:name=".FilterActivity"
            android:label="Фильтры" />
        <activity
            android:name=".CartActivity"
            android:label="Корзина" />
        <activity android:name=".PartActivity" />
        <activity
            android:name=".AdminActivity"
            android:label="Админ панель" />
        <activity
            android:name=".RegisterActivity"
            android:label="Регистрация"
            android:parentActivityName=".LoginActivity"
            android:theme="@style/LoginTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".LoginActivity"
            android:label="Авторизация"
            android:theme="@style/LoginTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>